{
  "collectionName": "components_shared_designs",
  "info": {
    "displayName": "Design",
    "icon": "brush",
    "description": "Per-section visual styling controls"
  },
  "options": {},
  "attributes": {
    "theme": {
      "type": "enumeration",
      "enum": ["light", "muted", "dark", "brand"],
      "default": "light"
    },
    "accentColor": {
      "type": "string",
      "default": "#5b34ea"
    },
    "align": {
      "type": "enumeration",
      "enum": ["left", "center"],
      "default": "center"
    },
    "paddingY": {
      "type": "enumeration",
      "enum": ["sm", "md", "lg"],
      "default": "lg"
    }
  }
}
