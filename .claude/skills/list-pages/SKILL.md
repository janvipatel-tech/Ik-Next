---
name: list-pages
description: List all CMS pages with their template and publish status (draft/published). Use when the user wants to see existing pages.
---

# List pages

1. Run `node scripts/list-pages.mjs`.
2. Present the result as a short table (slug, status, template, title).
