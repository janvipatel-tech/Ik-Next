---
description: Publish a CMS page draft so it goes live on the public site.
argument-hint: <slug>
---

Read `.claude/skills/publish-page/SKILL.md` and follow it exactly.

Slug: $ARGUMENTS

If no slug is given, run `node scripts/list-pages.mjs` and ask which page to publish.
